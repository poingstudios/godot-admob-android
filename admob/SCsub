Import('env')

sources = ['register_types.cpp', 'ios/src/AdMob.mm', 'ios/src/AdMobBanner.mm', 'ios/src/AdMobInterstitial.mm']

if (env["platform"] == "iphone"):
    env.add_source_files(env.modules_sources, sources)

