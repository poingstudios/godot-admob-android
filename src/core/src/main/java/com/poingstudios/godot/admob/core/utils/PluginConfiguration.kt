package com.poingstudios.godot.admob.core.utils

@Suppress("unused") // Used when create a new version in CI/CD.
class PluginConfiguration {
    object Constants {
        const val PLUGIN_VERSION = "4.0.0"
    }
}