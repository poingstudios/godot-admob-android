Import('env')

sources = ['register_types.cpp', 'src/AdMob.mm', 'src/AdMobBanner.mm', 'src/AdMobInterstitial.mm', 'src/AdMobRewarded.mm']

if (env["platform"] == "iphone"):
    env.add_source_files(env.modules_sources, sources)

